<IfModule mod_rewrite.c>
  RewriteEngine On
  Options -Indexes

  RewriteBase /Administration/public

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  RewriteRule ^(.*)$ index.php/$1
</IfModule>
